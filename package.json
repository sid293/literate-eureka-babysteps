{
  "name": "babysteps",
  "version": "1.0.0",
  "description": "BabySteps - Milestone Tracker & Community Tips",
  "scripts": {
    "start": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "install-deps": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
} 